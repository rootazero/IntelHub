# GEV P12 Flight Layer Parity — Ledger

> 日期：2026-09-21 · 状态：completed
> Worktree：`feat/p12-flight-layer-parity`（已 `--no-ff` 合并到 main）
> Base：`cd07a7b`（main HEAD，含 fix/deploy-stampede）· 最终 branch commit：`bf78096`（T9）+ docs commit（本 ledger）
> 上游 spec：`docs/superpowers/specs/2026-09-21-gev-p12-flight-layer-design.md`
> 实施计划：`docs/superpowers/plans/2026-09-21-gev-p12-flight-layer-implementation.md`

## 范围与交付（5 类）

- **A** hub-core `gev_enrichment.rs` — `/api/adsbdb/type/:hex` + `/api/adsbdb/route/:callsign` adsbdb 代理 + 24h disk cache（`/home/zou/IntelHub/data/`）+ dirty 15s flush + in-flight coalesce + 永久负缓存
- **B** hub-core `gev_tracks.rs` — `/api/opensky-track?icao24=` + `/api/adsblol/trace?hex=` + OpenSky OAuth（client_credentials）adaptive TTL + serve-stale + adsblol trace
- **C** console `aircraft-source.ts` — `getTrack` + `getEnrichment` 暴露给 vendor `standalone.js`（URL 路径字面量守住）
- **D** cockpit 5 子模块 aggressive UX 抛光：键盘快捷键 / camera flyTo 过渡 / briefing manual grace 5s + fade + 进度条 / panel-drag 锁定 / viewport lock
- **E** 验收：sp6 +2 + sp8 +5 + probe P12_PROBES + i18n

## 任务完成（10 任务）

| Task | 内容 | 结果 | commit |
|---|---|---|---|
| T1 | `gev_enrichment.rs` + 持久化 + route + 单测 | 8/8 tests | `6965cbf` |
| T1-r1 | tokio::fs + lib.rs 顺序 + start() 测试 | green | `b33b806` |
| T2 | `gev_tracks.rs` OpenSky OAuth + TTL + serve-stale | 10/10 tests | `38adcef` |
| T3 | console `aircraft-source.ts` + re-export + boot wiring | 16/16 tests | `e9e99c8` |
| T4 | source-contracts 路径字面量守卫 | 4/4 new guards | `3c9057d` |
| T5 | cockpit `shortcuts.ts` + i18n | 10/10 tests | `d4d9250` |
| T6 | cockpit `camera-transition.ts` flyTo | 6/6 tests | `40762d7` |
| T7 | cockpit `viewport-lock.ts` + `panel-drag` disabled | 10/10 tests (5+5) | `6c52ec4` |
| T8 | briefing manual grace 5s + fade + 进度条 | 3/3 tests | `d947235` |
| T9 | 验收扩展（sp6 +2 + sp8 +5 + probe P12_PROBES + i18n）+ 315 验收 | sp6 49+5sh/0f · sp8 54+2sh+3deferred/0f · sp3 19/0 · sp7 16+11sh/0f | `bf78096` |
| T10 | whole-branch review + ledger + merge + 410 + push | 本文件 | — |

## 测试总数

- backend（hub-core）：33 tests 全绿（gev_enrichment 8 + gev_tracks 10 + 既有回归 15）
- console（vitest）：549 tests 全绿
- **合计 582 tests**

## 315 验收（T9，Debian-test 10.10.10.35）

| 套件 | 结果 |
|---|---|
| sp6 | 49 passed · 5 shelved · 0 failed |
| sp8 | 54 passed · 2 shelved · 3 deferred · 0 failed |
| sp3 | 19 passed · 0 failed |
| sp7 | 16 passed · 11 shelved · 0 failed |

> 305s stampede wait honored on 315（AGENTS.md 2026-09-20 铁律）。

## 410 验收（T10 生产部署）

| 套件 | 结果 |
|---|---|
| sp6 | 49 passed · 5 shelved · 0 failed |
| sp8 | 54 passed · 2 shelved · 3 deferred · 0 failed |
| sp3 | 19 passed · 0 failed |
| sp7 | 16 passed · 11 shelved · 0 failed |

> 重启后强制 `sleep 300`（5 分钟 stampede 冷却）后验收。

## 决策日志（Rulings）

| 决策 | 上下文 | 影响 |
|---|---|---|
| **Probe exit 1 = pre-existing** | `console/probe-gev.mjs` 报 exit 1，根因是 `dismissSearch` Escape 递归（P10/P11-A 已 ship，AGENTS.md 有 precedent 记录），非 P12 引入 | 不修；P12 新增的 cockpit Escape 在 frame mount 前不做 dismissSearch 兜底 |
| 测试缓存路径从 `/var/lib/intelhub` 移到 `/home/zou/IntelHub/data` | systemd `ProtectSystem=strict` 会拒 `/var/lib/intelhub` 写入 | T1-r1 修正；disk cache 与部署目录同源 |
| AGENTS.md sp6 baseline `39+5sh` → `49+5sh` | T9 加 sp6 +2 检查位后 315 实测 | 基线更新（本次 docs commit） |
| AGENTS.md sp8 baseline `48+2sh` → `54+2sh+3deferred` | T9 加 sp8 +5 检查位；3 项 Playwright runtime 静态标 deferred | 基线更新；deferred 不计 failure |

## vendor 状态

`console/gev-engine/` 字节级 pin，本轮**零行改动**。验证：`git diff cd07a7b..HEAD -- console/gev-engine/` = empty。P12 不 fork vendor 代码，通过 source-contracts 测试钉住 URL 路径字面量 + 方法形状。

## 已知边界（保留，不修）

- adsbdb 数据不全（小型 GA tail 无 route/type）→ `{found:false}` 合法，UI 显示「未知」
- OpenSky `/tracks/all` 对未飞行 hex 返 404 → adapter silent fallback 到本地累积 trail
- 键盘快捷键 iframe 中需 `window.top`（未来 embed 才做）
- Touch 单指 tap 切换 briefing（`matchMedia('(pointer: coarse)')` 才激活）
- briefing manual nav 5s grace 期间 vendor 仍在 drip enrichment（无影响）
- cockpit 退出后 vision mode 持久化 `intelhub.cockpit.visionMode`（P9 已有）
- panel drag 锁定仅对当前 cockpit session 有效
- sp8 check_51-53（Playwright runtime）静态标 deferred，P13 处理
