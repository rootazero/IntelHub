# GEV P10 长尾移植 实施计划

> 日期：2026-09-18 · 状态：草稿（待 P10-T1 验证协议校正）
> 上游 spec：`docs/superpowers/specs/2026-09-18-gev-p10-tail-design.md`
> 上游总纲：`docs/superpowers/specs/2026-09-18-gev-visual-port-design.md`
> 同期姊妹：P6 `30a709b` · P7 `da166a1` · P8 `b29255a` · P9 `4a93022`

## 0. 范围与边界

**In scope**（一次性完整版 + T0 P9 deferred 4 项修复列）：
- T0（前置 commit）：NOAA unitCode 守门 / vision 重入修复 / BriefingHandle dead API 清理 / lib.rs secrets 字母序
- T1: source-contracts 8 模块钉扎 + baseline measure
- T2: console adapter 9 个 + ≥30 测试
- T3: HUD 5 子组件 + GlobeV2 wiring + 熵减
- T4: sp8 +3 + probe P10 段 + 315 验收
- T5: ledger + whole-branch final review + merge main + 410 + push

**Out of scope**：panelShell/director/scopeMask 实例化、cycleDetection、GEV 语音、services/application.js 6 slot 重构、P11+ deferred（详见 spec §8）

## 1. 总览

| Task | 内容 | 出口 | 文件 |
|---|---|---|---|
| **T0** | P9 deferred 4 项修复（前置 commit，不进 P10 PR） | 4 commit 各自独立；T1 验收前全绿 | `hub-core/.../gev_weather.rs` · `console/src/gev-visual/cockpit/cockpit-store.ts` · `.../briefing-mount.ts` · `hub-core/.../lib.rs` |
| **T1** | source-contracts + localStorage prefix 守卫 + baseline 测量 | 守卫绿（新增 14 模块/arity）；315 baseline sp8 报告 | `console/src/gev-boot/__tests__/source-contracts.test.ts` |
| **T2** | console adapter 9 个（panel-drag/panel-layout/panel-disclosure/scene-controls/scene-sharing/use-share-restoration/recording-controls/shortcuts/frame-rate-monitor）+ index + ≥30 测试 | vitest 全绿；mock 复刻真实构造器契约 | `console/src/gev-visual/tail/{9 files + index.ts + __tests__/}` |
| **T3** | HUD 5 子组件 + GlobeV2 wiring + 熵减 | console vitest 全绿；新 ≥10 测试；tsc rc=0；vendor 0 行 | `console/src/globe-hud/Hud{PanelDragHandle,ScenePanel,RecordingControls,ShortcutCheatsheet,FrameRateReadout}.tsx` + `pages/GlobeV2.tsx` + `hud.css` |
| **T4** | sp8 +3 + probe P10 段 + 315 验收 | sp8 48+2sh/0f · probe exit 0 | `scripts/accept-sp8.py` · `console/probe-gev.mjs` |
| **T5** | ledger + final review + merge + 410 + push | main @新 commit；410 验收全绿；pushed | `docs/superpowers/execution/2026-09-18-gev-p10-ledger.md` + `AGENTS.md` 一次性更新 |

## 2. Worktree 隔离

```bash
cd /Volumes/TBU/Workspace/IntelHub
git worktree add ../IntelHub-gev-p10 -b feat/gev-p10-tail
sleep 4
cd /Volumes/TBU/Workspace/IntelHub-gev-p10
git log -1 main   # 确认 BASE = 4a93022 (P9 ledger)
```

## 3. 任务详情

### Task 0 — P9 deferred 4 项修复列（前置 commit）

**Files**：
1. `hub-core/crates/hub-core/src/gev_weather.rs` — D1 NOAA unitCode 守门
2. `console/src/gev-visual/cockpit/cockpit-store.ts` — D2 vision 重入修复
3. `console/src/globe-hud/HudCockpitVisionSwitch.tsx` — D2 vision 重入
4. `console/src/gev-visual/cockpit/briefing-mount.ts` — D3 dead API 清理
5. `hub-core/crates/hub-core/src/lib.rs` — D4 secrets 字母序

**Step details**：
1. **D1 NOAA unitCode 守门**：
   - `parse_noaa_grid` 增加 unitCode 守门：读取 `properties.windSpeed.uom` 等字段，仅当 unitCode = `wmo Unit:km_h-1` 才应用 km/h→kts 转换；其他 unitCode 返回 null（不静默错值）
   - **活探前置**：实施者先 `ssh Debian-test 'curl -H "User-Agent: IntelHub/dev" "https://api.weather.gov/points/40,-74" | jq .'` 拿真实 response，确认 unitCode 字段存在且格式
2. **D2 vision 重入修复**：
   - `createCockpitStore`: `enter()` 后自动调 `vision.setMode(persistedVisionMode)` (optical no-op)
   - `HudCockpitVisionSwitch`: 同样在 mount 时同步应用（已读 `intelhub.cockpit.visionMode` localStorage）
   - 单测加 test：enter 后 assert `vision.getMode() === 'crt'` when persisted='crt'
3. **D3 BriefingHandle dead API 清理**：
   - 删除 `start()` / `stop()` / `pages` 属性（仅测试引用）
   - `briefing-mount.ts` 暴露 `next()` / `prev()` / `fetch()` / `destroy()` + 4 个 events
   - 测试更新：移除 dead API test，保留 next/prev/fetch test
4. **D4 lib.rs secrets 字母序**：
   - 检查 `pub mod` 列表，把 `secrets` 放到正确字母序位置（如 `secrets` 应该在 `request_services` 之后，`signal` 之前——按字母序）
   - 实施者先 `git show main:hub-core/crates/hub-core/src/lib.rs | head -50` 看当前顺序

**Verify**：
- 4 commit 各自独立
- T0 完成后 315 上 `cargo build --release` + `cd console && npx tsc -b` + 跑 P9 baseline（sp8 45+2sh/0f · sp6 39+5sh/0f）必须仍绿

### Task 1 — source-contracts + localStorage prefix 守卫 + baseline measure

**Files**：
1. `console/src/gev-boot/__tests__/source-contracts.test.ts` — 增 14 模块 + localStorage prefix 断言

**Steps**：
1. **baseline 测量**：315 上 sp8 跑 2 次（应该 = 45+2sh/0f 与 P9 一致；如有差异 ±1 阻塞）
2. **新增 14 模块钉扎**：
   - `panelDisclosure`、`panelMeasurement`、`panelRails`、`panelRailGeometry`（4 已有部分）
   - `sceneControls`、`sceneSharing`、`scenePresentation`、`shareRestoration`、`scenes`、`recordingControls`、`applicationShortcuts`、`frameRateMonitor`、`scenePolicy`（9 新）
   - `services/application`（1 service 模块）
3. **localStorage prefix 守卫**：断言 `godsEyeView.v6.panelPos.*` / `godsEyeView.v6.panelCollapsed.*` 前缀存在（vendor 沿用；T1 review 已知 ledger:123 修正 `layoutRightPanels` → `panelCollapsed.<id>`）
4. **P9 deferred T0 验收**：检查 cockpitMath/cockpitVisionPolicy 守卫仍绿；source-contracts 总数 +14
5. **mutate probe**：翻转 BRIEF_PAGES（已 3 pins）应仍 fail；翻转 panel position localStorage key 名应 fail

**Verify**：
- `npx vitest run src/gev-boot/__tests__/source-contracts.test.ts` 全绿
- baseline sp8 = 45+2sh/0f ×2
- vendor 0 行改动

### Task 2 — console adapter 9 个 + ≥30 测试

**Files**：
1. `console/src/gev-visual/tail/panel-drag.ts`（新）
2. `console/src/gev-visual/tail/panel-layout.ts`（新）
3. `console/src/gev-visual/tail/panel-disclosure.ts`（新）
4. `console/src/gev-visual/tail/scene-controls.ts`（新）
5. `console/src/gev-visual/tail/scene-sharing.ts`（新）
6. `console/src/gev-visual/tail/use-share-restoration.ts`（新，D1 方案 A：丢 vendor module 写 React hook）
7. `console/src/gev-visual/tail/recording-controls.ts`（新）
8. `console/src/gev-visual/tail/shortcuts.ts`（新）
9. `console/src/gev-visual/tail/frame-rate-monitor.ts`（新）
10. `console/src/gev-visual/tail/index.ts`（新）
11. 9 个 `__tests__/` 新测试

**Adapter 签名**（实施者先打开 vendor 验证真实构造器）：
- `PanelDragHandle{ mount(panel, opts): Handle; destroy() }` —— 包装 panelPositionControls
- `PanelLayout{ layout(stack, opts): Promise<void>; destroy() }` —— 包装 leftPanelRail + rightPanelRail
- `PanelDisclosure{ bind(button, panel): Handle; destroy() }` —— 包装 panelDisclosure
- `SceneControls{ mount(panel, opts): Handle; destroy() }` —— 包装 sceneControls
- `SceneSharing{ mount(panel, opts): Handle; destroy() }` —— 包装 sceneSharing
- `useShareRestoration() { urlState, encode(state), decode(hash) }` —— React hook（D1 方案 A，URL hash 编码/恢复纯 intelhub state）
- `RecordingControls{ mount(opts): Handle; destroy() }` —— 包装 recordingControls + HUD 契约
- `Shortcuts{ bind(opts): Handle; destroy() }` —— 包装 applicationShortcuts + 8 action 映射
- `FrameRateMonitor{ mount(viewer, opts): Handle; destroy() }` —— 包装 frameRateMonitor

**T2 验证协议**：
- 实施者必须先打开每个 vendor 文件，确认 export 名 + 构造器形状
- 与 T0 已修的 cockpit-store + HudCockpitVisionSwitch 集成：shortcuts.setStyle action 必须 R5 gate（`!cockpitStore.active`）
- panel-layout 必须 strip vendor obstacle selectors 不适用 IntelHub DOM 的（如 `#gev-voice-control` `#clean-view-exit`）—— T2 实施者用 selector mapping table 决定保留/删除

**Verify**：
- `npx vitest run src/gev-visual/tail/` 全绿（≥30 测试）
- vendor 0 行改动
- mock 复刻真实构造器契约（P3 教训）

### Task 3 — HUD 5 子组件 + GlobeV2 wiring + 熵减

**Files**：
1. `console/src/globe-hud/HudPanelDragHandle.tsx`（新）
2. `console/src/globe-hud/HudScenePanel.tsx`（新）
3. `console/src/globe-hud/HudRecordingControls.tsx`（新）
4. `console/src/globe-hud/HudShortcutCheatsheet.tsx`（新）
5. `console/src/globe-hud/HudFrameRateReadout.tsx`（新）
6. `console/src/globe-hud/HudLayerRail.tsx`（改）—— 加 4 个新图标（scene / recording / shortcuts / fps readout toggle）
7. `console/src/pages/GlobeV2.tsx`（改）—— 挂载 9 个 tail adapter + 5 个 HUD + cleanup order
8. `console/src/hud.css`（改）—— `.hud-tail-*` 类
9. 5 个 `__tests__/` 新测试（+10 测试）
10. `console/gev-engine/src/styles/recording.css` + `scenes.css`（vendor 资源**整体 import** 到 `console/src/hud.css` 顶部或独立 import）

**Step details**：
1. **GlobeV2 接线**：
   - cleanup order：tail (frame-rate + shortcuts + recording + scene-controls + share-restoration + panel-drag + panel-layout + panel-disclosure + scene-sharing) FIRST → cockpit → annotation → follow → camera → visual-effects → globe.destroy()
   - HUD 契约：`this.hud = { getMode, getVariant, setMode, setVariant, visible }` — adapter 通过 callback 提供给 recordingControls
2. **shortcuts R5 兼容性**：setStyle action 检查 `!cockpitStore.active`（与 P9 一致）
3. **localStorage 命名空间**：adapter 暴露 `setPanelPos(panelId, x, y)` 函数封装 vendor `godsEyeView.v6.panelPos.<id>` key
4. **scene-share hook**：与 GlobeV2 现有 URL restoration 路径协同；新 hook 仅 encode/decode 视觉状态（style + camera + tracking + selected + panel layout），不影响其他 hash 参数
5. **testids 逐字**：`hud-panel-drag-handle` / `hud-scene-panel` / `hud-recording-toggle` / `hud-recording-mode-active` / `hud-shortcut-cheatsheet` / `hud-shortcut-key-{name}` / `hud-frame-rate-readout` / `hud-scene-capture-button` / `hud-scene-share-link`
6. **熵减 grep**：`grep -rn "TODO.*panel\|TODO.*scene\|TODO.*recording\|placeholder.*panel\|P5\|P10" console/src/` 应为空（vendor + adapter comments 例外）
7. **vendor CSS 整体 import**：recording.css + scenes.css 直接复制到 hud.css 顶部并加注释（vendor 来源）

**Verify**：
- `npx vitest run src/globe-hud/` 全绿（含 baseline 4 失败）
- 新 ≥10 测试
- `npx tsc -b` rc=0
- vendor 0 行改动（vendor CSS 复制到 hud.css 是允许的——不算 vendor 修改）

### Task 4 — sp8 +3 + probe P10 段 + 315 验收

**Files**：
1. `scripts/accept-sp8.py`（改）—— +3 检查
2. `console/probe-gev.mjs`（改）—— 增 P10 段
3. （如有）`scripts/_remote.py` `req()` 沿用

**Step details**：
1. **rsync → Debian-test**：exclude 清单 + `.superpowers/`
2. **build-hub.sh + build-console.sh + restart hub-core**
3. **probe P10 段**：`p10-frame-rate-readout=1` + `p10-shortcut-cheatsheet=1` + `p10-scene-panel=1` + `p10-recording-mode=1` + `p10-panel-drag=<id>=1`
4. **sp8 +3 检查**：
   - `recording_mode_body_class`: 切换录制模式后 `body.recording-mode` class 存在
   - `scene_panel_exists`: HUD scene panel DOM 存在
   - `panel_localStorage_roundtrip`: panel 拖拽 → localStorage 写入 → reload 后位置恢复
5. **跑 sp3 + sp6 + sp7 验回归**

**Verify**：
- 315 sp8 48+2sh/0f · sp3 19/0 · sp6 39+5sh/0f · sp7 16+11sh/0f
- probe exit 0
- 报告含 4 个 SP 数 + probe P10 段明细

### Task 5 — ledger + final review + merge + 410 + push

**Files**：
1. `docs/superpowers/execution/2026-09-18-gev-p10-ledger.md`（新）
2. `AGENTS.md`（改）—— sp8 45+2sh → 48+2sh（315 clean baseline）+ 已知坑补 P10 条目

**Step details**：
1. **whole-branch final review**：dispatch deepseek-v4-pro subagent（kimi 限额 ruling）。提供 spec + plan + 全 diff + T0-T4 reports + reviews。Ask: coherence / type consistency / entropy check / 9 deferred minors triage（含 T0 4 项落地 + T2 panel-layout selector 删减 + T4 recording body class）
2. **AGENTS.md update**：仅改 sp8 行；已知坑补 P10 记录（localStorage 前缀策略 + scene share hook D1 选择理由 + T0 4 项结果）
3. **git merge --no-ff**：feat/gev-p10-tail → main
4. **rsync → IntelHub (410)**：exclude 清单
5. **410 build-hub.sh + build-console.sh + restart hub-core**
6. **410 acceptance**：probe + sp6/sp7/sp8/sp3
7. **git push origin main**

**Verify**：
- 410 sp8 51+2sh/0f · sp7 16+11sh/0f · sp6 39+5sh/0f（or overpass flap 38+5sh/1f）· sp3 19/0
- 410 probe exit 0
- ledger commit + push 双 commit 已发
- Final review verdict（APPROVE/MINORS/REJECT）

## 4. 风险与缓解

| # | 风险 | 缓解 |
|---|---|---|
| R1 | T0 deferred 4 项回归风险 | T0 完成后跑 P9 baseline + cargo build + tsc 必须绿 |
| R2 | shareRestoration.js D1 方案 A 写 hook 与 GlobeV2 URL restoration 冲突 | T2 实施者先读 GlobeV2 现有 hash 解析路径；hook 仅管视觉状态子集 |
| R3 | panel-layout obstacle selector 删除导致 layout 偏移 | T2 selector mapping table 走 "preserve-or-delete" 二元决策，保留必要 + 删除 vendor DOM 不存在的 |
| R4 | shortcuts.setStyle 与 P9 R5 守门冲突 | T3 显式集成 R5 gate（`!cockpitStore.active`） |
| R5 | vendor CSS recording.css + scenes.css wholesale import | T3 整体复制到 hud.css 顶部，注释标记来源；CSS 选择器具体性已验（vendor 更高，无冲突） |
| R6 | localStorage 命名空间 churn（vendor 前缀） | P10 spec 已决定沿用 `godsEyeView.v6.*` 避免首次 churn；P11+ 整理 |
| R7 | kimi 限额 403 weekly | 同 P7/P8/P9 ruling，用 deepseek-v4-pro 做 final review |
| R8 | T4 sp8 panel_localStorage_roundtrip 实测 | probe jsdom 中 localStorage 写入 + 读出验证 |

## 5. 验证协议

每 task 实施者须 dispatch 后**先打开 vendor + 已上线代码**，纠正 plan 错误：
- T1: vendor panel/scene/shortcut export 名（plan 列了 14 模块，真实可能有调整）
- T2: vendor PanelChrome 是否真的"不实例化"（plan §0 决策；T2 应验证不直接 import 任何 composition root 类）
- T3: HUD contract `this.hud.{getMode,getVariant,setMode,setVariant,visible}` 真正存在哪（可能 vendor 没有现成 adapter，需要 React wrapper）

每 task 报告 → `/Volumes/TBU/Workspace/IntelHub-gev-p10/.superpowers/sdd/2026-09-18-gev-p10-tail/task-{N}-report.md`

## 6. 熵减清单（task 1 / 3 / 5 必做）

- `grep -rn "TODO.*panel\|TODO.*scene\|TODO.*recording\|placeholder.*panel" console/src/` 应为空（vendor + adapter comments 例外）
- `grep -rn "panelChrome\|shellFacade\|director\|applicationShell" console/src/` 仅适配层 comment 允许
- `grep -rn "godsEyeView" console/src/` 应仅出现在 adapter layer（panel-drag.ts）+ 一处 import vendor CSS 注释
- 删除 P9 deferred dead API（BriefingHandle.start/stop/pages）+ P9 旧的 cockpit tests

## 7. 任务派发模板

每 task dispatch：
```bash
/Users/zouguojun/.pi/agent/git/github.com/obra/superpowers/skills/subagent-driven-development/scripts/dispatch-task \
  --task task-{N} --base {prev-commit-or-base} \
  --model deepseek-v4-flash  # 或 deepseek-v4-pro
```

Brief 含：plan 链接 + 验证协议 + cross-task context + 工作目录 + 验证命令 + 报告路径。