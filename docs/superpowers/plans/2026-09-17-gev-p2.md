# GEV 引擎融合 P2（引擎基座 + 波次 1 五图层）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Vendor gods-eye-view 引擎进 IntelHub console，自研 bootstrap + IntelHub 数据适配器 + 手游式 React HUD 骨架，点亮波次 1 五图层（航班/军机/卫星/地震/3D 城市）。

**Architecture:** 四层分工（spec §1）：React HUD 壳 → 纯净 vendored 引擎（`console/gev-engine/`）→ IntelHub 适配器（`console/src/gev-adapters/`，实现 SOURCE_METHODS 契约）→ hub-core REST（`/api/v1/gev/*` + 复用 `/api/v1/globe/*`）。自研 bootstrap（`console/src/gev-boot/`）调引擎通用 4 阶段生命周期 `createApplication`，**不使用** `src/standalone/` 任何模块。

**Tech Stack:** React 19 + TS + Vite 6 + vitest（console 新增 dev-dep）；Rust hub-core（零新 crate）；Cesium ^1.124（resolve 1.145）+ satellite.js ^6.0.2（已有）。

**Spec:** `docs/superpowers/specs/2026-09-17-gev-engine-fusion-design.md`

## Global Constraints

- worktree `/Volumes/TBU/Workspace/IntelHub-gev`（branch `feat/gev-engine`）；严禁直接动 main 工作区
- **引擎纯净度铁律**：`console/gev-engine/` 内不得出现任何本地修改；所有接缝在 `console/src/gev-boot/` 与 `console/src/gev-adapters/`
- Rust 零新 crate 依赖；console 新增 dev-dep 仅限 `vitest` + `@testing-library/react` + `@testing-library/jest-dom` + `jsdom`
- secrets 只在 VM；新 env 走 `core/secrets.env`（hub）/ `core/console-build.env`（console build）链，rsync exclude 已挡
- 上游 API 探测一律从 VM 发（`ssh Debian-test`）
- 验收基线全绿：sp6（27 项基线）/sp8/sp7/sp3 + probe-gev；T17 后更新 AGENTS.md
- T17 完成 315 验收后**必须暂停**，merge→410→push 由用户放行（T18）
- `HubError` 无 `From<serde_json::Error>`/`From<sqlx::Error>`，用 `HubError::sensor(format!(...))`
- React 无 error boundary 会黑根；引擎单例（一页一实例）+ StrictMode 双挂载必须防护
- 引擎代码是 JS（无类型）——适配器/HUD 全部 TS，引擎 import 走 `// @ts-expect-error` 局部声明文件（T2 的 `gev-engine.d.ts`）

## 关键接口锚点（全部任务共享，提取自上游源码，执行时以 vendor 内实际代码复核）

- 生命周期：`createApplication({createScene, createControls, createData, createTools})`（`src/app/application.js:12`）。各 factory 收 `{...components, signal, defer}`，defer 必须同步注册清理；返回 `{start(), destroy(), getState(), getComponents(), subscribe(listener)}`
- 场景：`createApplicationScene({requestServices, googleApiKey, cesiumToken, loaderStatus, signal, defer})`（`src/app/scene.js:16`）→ `{viewer, tileset, mapStackController, operations}`；`operations = {requests, surface, annotationResolver, searchAndFlyTo}`（`src/app/operations.js:34-48`）
- requestServices 必备面（`src/services/requests.js:13`，`src/app/operations.js:10-17` 校验）：`boundaries.query`、`terrain.getHeights`、`regional.getBrief`、`weather.getConditions`、`summary.summarize`、`features`
- 目录：`createApplicationCatalog({surface, sources, signal, metadata?, resolveAsset?, ...})`（`src/app/constructCatalog.js:53`）→ 冻结 `{...catalog, militaryRegistry, surface}`。**SOURCE_METHODS 全部 14 层都必须存在且方法齐全**（`constructCatalog.js:26-47,68-74` 逐个校验，缺任一层抛 `TypeError`）：
  `flights/military/vessels/firms/earthquakes:['getSnapshot']`、`cctv:['getCatalog','getHealth','getFrameUrl','getMediaUrl']`、`radio:['getDirectory','recordClick']`、`traffic:['requestRoads','getStatus','fetchFlowForBounds','getFlowSessionStats','resetFlowTileCache']`、`bikeshare:['getStations']`、`installations:['getMappedSites','searchNearby']`、`satellites:['readGroup']`、`launches:['getLaunches','getActiveTle']`、`alpr:['fetch']`、`cables:['fetch']`
- scopeMask：`setScopeMaskEnabled(false)`（`src/scopeMask.js:504`）；引擎在 `src/app/tools.js:66-68` 安装它，start 后调用关闭即可
- 必需 DOM：`#loading-screen > .loader-status`、`#cesiumContainer`、`#data-toggles`（`src/app/data.js:56`）；`#cesium-credits` 由 scene factory 自建
- Viewer 构造（`src/app/viewer.js:7-22`）：timeline/animation/baseLayerPicker 等全 false、`baseLayer:false`、`msaaSamples:4`、`preserveDrawingBuffer:true`；Ion token 不设全局（per-call）；terrain 故意禁用
- 航班/军机记录（`src/sources/live/aircraft.js:9-71`）：`{id, reference, latitude, longitude, callsign, originCountry, positionTimeMs, contactTimeMs, baroAltitudeM, ellipsoidAltitudeM, onGround, speedMps, courseDeg, verticalRateMps, category, typeCode, registration, operator}`；envelope（`aircraft.js:85-112`）：`{records, complete, rejectedCount, source, coverage, observedAtMs, ageMs, stale, freshness, status}`；可选 `getTrack(reference,{signal})`（不存在则 layer 用 `getTrack?.()` 跳过）、军机可选 `getIdentities`
- 卫星：`readGroup(group,{signal})` → `{ok, status, text}`（raw TLE 文本）；组集合硬编码 `['stations','visual','gps-ops','glo-ops','galileo','geo','starlink']`（`src/layers/satellites/source.js:10-24`），未知组必须抛 `TypeError('Unknown satellite group')`
- 地震：`getSnapshot({signal})` → **纯数组** `[{stableId, usgsId, lon, lat, depthKm, mag, place, time}]`（`src/layers/earthquakes/records.js:37-46`）；mag null 或 <2.5 的行会被 layer 丢弃
- 3D 城市：引擎自带 `src/maps/google3d.js` 回退链（google-direct → ion 2275207 → osm），`selectMapStartupRoute({googleApiKey, cesiumToken})`；**无需适配器**，只需 `googleApiKey` 传进 scene
- P1 adsb 快照（Redis `hub:globe:aircraft`，TTL 300s）：envelope `{ts, count, coverage, cycle_secs, last_tick, aircraft:[{hex, flight, lat, lon, alt_m, gs, track, squawk, mil, seen, age_s}], stale?}`；**`gs` 单位是节（knots）**，`alt_m` 米，`track` 度，`age_s` 秒
- P1 celestrak 现组：`["stations","visual","weather","gnss","military"]`（T5 改为 GEV 对齐）

---

### Task 1: Vendor 引擎导入 + UPSTREAM pin + 同步脚本

**Files:**
- Create: `console/gev-engine/`（rsync 自上游 pin 版本，~全部 `src/` 减去排除项）
- Create: `console/gev-engine/UPSTREAM.json`、`scripts/sync-gev-engine.sh`、`NOTICE`
- Modify: `.gitattributes`（vendor 标记 linguist-vendored）

**Interfaces:**
- Produces: `console/gev-engine/src/app/application.js` 等引擎源码（pin 版本）；`UPSTREAM.json = {repo, pinned_sha, synced_at}`；同步脚本幂等可重跑

- [ ] **Step 1: 确定 pin SHA 并写入 UPSTREAM.json**

```bash
cd /Volumes/TBU/Github/gods-eye-view && git log -1 --format="%H %cI"
```

把输出的 full SHA 与日期写入 `console/gev-engine/UPSTREAM.json`：

```json
{
  "repo": "https://github.com/bilawalsidhu/gods-eye-view",
  "pinned_sha": "<上一步的 full SHA>",
  "synced_at": "<YYYY-MM-DD>",
  "excludes": [".git", "server", "src/standalone", "src/ui/templates", "src/data/local_data", "docs", "build", "index.html", "style.css", "public"]
}
```

- [ ] **Step 2: 写同步脚本**

`scripts/sync-gev-engine.sh`（幂等；fresh clone → checkout pin → rsync → 守卫提示）：

```bash
#!/usr/bin/env bash
# Sync vendored GEV engine from upstream pinned SHA. Idempotent.
# Usage: bash scripts/sync-gev-engine.sh [--check]
set -euo pipefail
ROOT="$(cd "$(dirname "$0")/.." && pwd)"
VENDOR="$ROOT/console/gev-engine"
META="$VENDOR/UPSTREAM.json"
SHA=$(python3 -c "import json;print(json.load(open('$META'))['pinned_sha'])")
TMP=$(mktemp -d)
trap 'rm -rf "$TMP"' EXIT
git clone --quiet --filter=blob:none https://github.com/bilawalsidhu/gods-eye-view "$TMP/gev"
git -C "$TMP/gev" checkout --quiet "$SHA"
if [[ "${1:-}" == "--check" ]]; then
  REMOTE=$(git -C "$TMP/gev" ls-remote origin HEAD | cut -f1)
  [[ "$REMOTE" == "$SHA" ]] && echo "gev-engine: up to date ($SHA)" || echo "gev-engine: upstream moved HEAD=$REMOTE pinned=$SHA"
  exit 0
fi
rsync -a --delete \
  --exclude '.git' --exclude 'server' --exclude 'src/standalone' \
  --exclude 'src/ui/templates' --exclude 'src/data/local_data' \
  --exclude 'docs' --exclude 'build' --exclude 'index.html' \
  --exclude 'style.css' --exclude 'public' --exclude 'UPSTREAM.json' \
  "$TMP/gev/" "$VENDOR/"
echo "synced gev-engine @ $SHA — now run: cd console && npx vitest run src/gev-boot/__tests__/source-contracts.test.ts"
```

注意：脚本**不修改** UPSTREAM.json——pinned_sha 更新是显式手工决策（改 JSON 后重跑脚本）。UPSTREAM.json 里的 excludes 字段是文档性的，与脚本的 rsync exclude 列表保持手工同步。

- [ ] **Step 3: 执行首次同步**

```bash
bash scripts/sync-gev-engine.sh && ls console/gev-engine/src/app/application.js console/gev-engine/src/scopeMask.js console/gev-engine/LICENSE
```

Expected: 三个文件都存在；`console/gev-engine/src/standalone/` 与 `console/gev-engine/src/ui/templates/` **不存在**

- [ ] **Step 4: .gitattributes + NOTICE**

`.gitattributes` 追加：

```
console/gev-engine/** linguist-vendored
```

`NOTICE`（仓库根，已存在则追加段落）：

```
gods-eye-view — MIT License — Copyright (c) Bilawal Sidhu
Vendored under console/gev-engine/ (see console/gev-engine/LICENSE).
Bundled upstream data packs (CC BY-NC-SA submarine cables, ODbL OSM
extracts) are NOT vendored; equivalent data is ingested into IntelHub
with attribution (see docs/superpowers/specs/2026-09-17-gev-engine-fusion-design.md §2).
```

- [ ] **Step 5: 验证纯净度基线 + Commit**

```bash
cd console/gev-engine && git ls-files --others --exclude-standard | head -3  # 应为空（全部 tracked）
cd /Volumes/TBU/Workspace/IntelHub-gev && git add -A && git commit -m "chore(gev): vendor engine @ <short-sha> (pristine, pinned) + sync script"
```

---

### Task 2: Vite 集成 + 引擎 import 打通

**Files:**
- Modify: `console/vite.config.ts`
- Create: `console/src/gev-boot/gev-engine.d.ts`
- Modify: `console/package.json`（devDeps: vitest, @testing-library/react, @testing-library/jest-dom, jsdom）
- Create: `console/vitest.config.ts`

**Interfaces:**
- Produces: `import { createApplication } from "../../gev-engine/src/app/application.js"` 在 console TS 代码中可编译；`vite build` 绿

- [ ] **Step 1: 安装测试工具链**

```bash
cd console && npm install -D vitest @testing-library/react @testing-library/jest-dom jsdom
```

- [ ] **Step 2: vitest.config.ts**

```ts
import { defineConfig } from "vitest/config";
import react from "@vitejs/plugin-react";
export default defineConfig({
  plugins: [react()],
  test: { environment: "jsdom", globals: true, setupFiles: ["./src/test-setup.ts"] },
});
```

`console/src/test-setup.ts`：`import "@testing-library/jest-dom";`

- [ ] **Step 3: vite.config.ts 加 define（引擎读 `import.meta.env.GOOGLE_MAPS_API_KEY/CESIUM_ION_TOKEN`，见 build/vite.js:33-36 的等价物）**

```ts
export default defineConfig({
  plugins: [react(), tailwindcss(), cesium({ rebuildCesium: true })],
  define: {
    "import.meta.env.GOOGLE_MAPS_API_KEY": JSON.stringify(process.env.VITE_GOOGLE_MAPS_KEY ?? ""),
    "import.meta.env.CESIUM_ION_TOKEN": JSON.stringify(process.env.VITE_ION_KEY ?? ""),
  },
  build: { outDir: "dist", sourcemap: false },
});
```

注意：build-console.sh 的 docker 构建会把 `VITE_GOOGLE_MAPS_KEY`/`VITE_ION_KEY` 传入（P1 已建链，T17 验证 `.build-manifest.json` 含这两个字段）。

- [ ] **Step 4: gev-engine.d.ts（引擎 JS 模块的最小声明）**

```ts
declare module "*gev-engine*" { const anyExport: any; export = anyExport; }
```

若 TS 仍报错，改为逐模块 `declare module "../../gev-engine/src/app/application.js" { export function createApplication(x: any): any; }` 形式按实际 import 逐个声明（执行时以 tsc 输出为准补全）。

- [ ] **Step 5: import 冒烟 + 构建验证**

```bash
cd console && npx tsc --noEmit && npm run build 2>&1 | tail -3
```

临时在 `src/main.tsx` 顶部加一行 `import "../../gev-engine/src/app/application.js";` 验证打包通过后**删除**（引擎入口由 T7 bootstrap 正式引入）。Expected: build 成功，无 unresolved import

- [ ] **Step 6: Commit**

```bash
git add -A && git commit -m "feat(gev): vite/vitest integration for vendored engine (define keys, d.ts, test toolchain)"
```

---

### Task 3: 适配器骨架 + 10 个契约 stub + 工厂

**Files:**
- Create: `console/src/gev-adapters/types.ts`、`stubs.ts`、`http.ts`、`index.ts`
- Test: `console/src/gev-adapters/__tests__/stubs.test.ts`

**Interfaces:**
- Consumes: T2 的引擎 import 路径
- Produces: `createIntelHubLayerSources(deps: {apiFetch: (path: string, init?: RequestInit) => Promise<Response>}): Record<string, object>`——返回满足全部 14 层 SOURCE_METHODS 的 sources 对象；wave-1 四层由 T4-T6 提供真实实现并在此注册

- [ ] **Step 1: 失败测试——stub 覆盖全部 14 层**

`__tests__/stubs.test.ts`：

```ts
import { createIntelHubLayerSources } from "../index";
const REQUIRED: Record<string, string[]> = {
  flights: ["getSnapshot"], military: ["getSnapshot"], vessels: ["getSnapshot"],
  cctv: ["getCatalog","getHealth","getFrameUrl","getMediaUrl"],
  radio: ["getDirectory","recordClick"],
  traffic: ["requestRoads","getStatus","fetchFlowForBounds","getFlowSessionStats","resetFlowTileCache"],
  bikeshare: ["getStations"], installations: ["getMappedSites","searchNearby"],
  satellites: ["readGroup"], launches: ["getLaunches","getActiveTle"],
  alpr: ["fetch"], firms: ["getSnapshot"], earthquakes: ["getSnapshot"], cables: ["fetch"],
};
test("sources cover every SOURCE_METHODS entry", () => {
  const s = createIntelHubLayerSources({ apiFetch: async () => new Response("{}") });
  for (const [layer, methods] of Object.entries(REQUIRED))
    for (const m of methods)
      expect(typeof (s as any)[layer]?.[m], `${layer}.${m}`).toBe("function");
});
test("stub getSnapshot resolves to empty-degraded envelope", async () => {
  const s = createIntelHubLayerSources({ apiFetch: async () => new Response("{}") });
  const env = await (s as any).vessels.getSnapshot({}, {});
  expect(env.records).toEqual([]); expect(env.status).toBe("unavailable");
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd console && npx vitest run src/gev-adapters` Expected: FAIL（模块不存在）

- [ ] **Step 3: 实现 types.ts + stubs.ts + http.ts + index.ts**

`types.ts`：

```ts
export interface SnapshotEnvelope<T = unknown> {
  records: T[]; complete: boolean; rejectedCount: number;
  source: string; coverage?: unknown; observedAtMs: number; ageMs: number;
  stale: boolean; freshness: "live" | "stale" | "unavailable"; status: "ok" | "degraded" | "unavailable";
}
export const emptyEnvelope = (source: string): SnapshotEnvelope => ({
  records: [], complete: false, rejectedCount: 0, source,
  observedAtMs: 0, ageMs: 0, stale: true, freshness: "unavailable", status: "unavailable",
});
```

`stubs.ts`：为 vessels/firms/cables/alpr/launches/cctv/radio/traffic/bikeshare/installations 各导出 contract-valid stub。getSnapshot 类返 `emptyEnvelope("intelhub-stub:<layer>")`；`satellites.readGroup` 之外的 read* 返 `{ok:false, status:501, text:""}`；`cctv.getFrameUrl/getMediaUrl` 返 `""`、`traffic.fetchFlowForBounds` 返 `{tiles:0,segments:[]}` 等——**返回形状执行时对照 `src/app/constructCatalog.js` 的 SOURCE_METHODS 语义与对应 layer 的 ingestion 容错路径逐个确认**（stub 目标是「图层可开启但显示空」，不是伪造数据）。

`http.ts`：

```ts
export type ApiFetch = (path: string, init?: RequestInit) => Promise<Response>;
export const makeApiFetch = (base: string, token: string): ApiFetch =>
  (path, init) => fetch(`${base}${path}`, {
    ...init, headers: { Authorization: `Bearer ${token}`, ...(init?.headers ?? {}) },
  });
```

`index.ts`：

```ts
import { emptyEnvelope } from "./types";
import * as stubs from "./stubs";
export function createIntelHubLayerSources(deps: { apiFetch: import("./http").ApiFetch }) {
  return {
    flights: stubs.flights,        // T6 替换为真实实现
    military: stubs.military,      // T6 替换
    satellites: stubs.satellites,  // T5 替换
    earthquakes: stubs.earthquakes,// T4 替换
    vessels: stubs.vessels, firms: stubs.firms, cables: stubs.cables,
    alpr: stubs.alpr, launches: stubs.launches, cctv: stubs.cctv,
    radio: stubs.radio, traffic: stubs.traffic, bikeshare: stubs.bikeshare,
    installations: stubs.installations,
  };
}
```

（stubs.flights 等四个也先给 emptyEnvelope 版，T4-T6 逐一替换 index.ts 注册。）

- [ ] **Step 4: 测试通过 + Commit**

Run: `npx vitest run src/gev-adapters` Expected: PASS
`git commit -m "feat(gev): adapter skeleton + contract stubs for 14 layers"`

---

### Task 4: 地震端点 `/api/v1/gev/earthquakes` + 适配器

**Files:**
- Modify: `hub-core/crates/hub-core/src/api.rs`（新 handler + 路由）
- Test: `hub-core/crates/hub-core/src/api.rs` 内 `#[cfg(test)]`（跟随现有 globe handler 测试模式）
- Create: `console/src/gev-adapters/earthquakes.ts`
- Modify: `console/src/gev-adapters/index.ts`（注册真实实现）
- Test: `console/src/gev-adapters/__tests__/earthquakes.test.ts`

**Interfaces:**
- Consumes: `geo_events` 表（`kind='quake'`，`payload` 为 USGS properties：`mag/place/time`；usgs.rs `Signal::new("quake", "M{mag} — {place}", lat, lon, id)`，external_id=USGS feature id）
- Produces: `GET /api/v1/gev/earthquakes` → JSON 数组 `[{stableId, usgsId, lon, lat, depthKm, mag, place, time}]`；适配器 `earthquakesSource.getSnapshot({signal})` 返同形数组

- [ ] **Step 1: Rust 失败测试（行映射纯函数）**

```rust
#[test]
fn quake_row_maps_geo_event() {
    let ev = serde_json::json!({"mag": 5.1, "place": "10km S of X", "time": 1758000000000i64});
    let row = quake_row("ext-1".into(), "ext-1".into(), 140.1, 35.2, Some(ev), "2026-09-17T00:00:00Z".into());
    assert_eq!(row["mag"], 5.1); assert_eq!(row["stableId"], "ext-1");
    assert_eq!(row["time"], 1758000000000i64); assert!(row["depthKm"].is_null());
}
```

- [ ] **Step 2: 实现 handler（紧随现有 `console_globe_satellites` 模式）**

```rust
// api.rs — 路由表加:
.route("/api/v1/gev/earthquakes", get(gev_earthquakes))

fn quake_row(stable_id: String, usgs_id: String, lon: f64, lat: f64,
             payload: Option<serde_json::Value>, occurred: String) -> serde_json::Value {
    let p = payload.unwrap_or_default();
    serde_json::json!({
        "stableId": stable_id, "usgsId": usgs_id, "lon": lon, "lat": lat,
        "depthKm": p.get("depthKm").cloned().unwrap_or(serde_json::Value::Null),
        "mag": p.get("mag").cloned().unwrap_or(serde_json::Value::Null),
        "place": p.get("place").cloned().unwrap_or(serde_json::Value::Null),
        "time": p.get("time").cloned().unwrap_or(serde_json::Value::Null),
        // occurred fallback: 若 payload.time 缺失由 SQL COALESCE 保证 time 非空策略见下
    })
}

async fn gev_earthquakes(State(state): State<AppState>) -> Result<Json<serde_json::Value>, HubError> {
    let rows = sqlx::query_as::<_, (String, String, f64, f64, Option<serde_json::Value>, String)>(
        r#"SELECT external_id, external_id, lon, lat, payload,
                  to_char(occurred_at AT TIME ZONE 'UTC', 'YYYY-MM-DD"T"HH24:MI:SS"Z"')
           FROM geo_events
           WHERE kind = 'quake' AND occurred_at > now() - interval '24 hours'
             AND lat IS NOT NULL AND lon IS NOT NULL
           ORDER BY occurred_at DESC LIMIT 500"#,
    )
    .fetch_all(&state.pg).await
    .map_err(|e| HubError::sensor(format!("gev earthquakes query: {e}")))?;
    let out: Vec<_> = rows.into_iter()
        .map(|(a,b,x,y,p,t)| quake_row(a,b,x,y,p,t)).collect();
    Ok(Json(serde_json::Value::Array(out)))
}
```

注意：USGS `payload` 是 properties 克隆（含 mag/place/time 毫秒 epoch）；`depthKm` 不在 properties（在 geometry），返回 null（契约允许 nullable）。`mag<2.5` 行由引擎 layer 自行丢弃，端点不过滤。

- [ ] **Step 3: `cargo test -p hub-core quake_row` 通过**

- [ ] **Step 4: 适配器（TDD：mock apiFetch 断言透传数组 + 非数组防御返 `[]`）**

```ts
export const earthquakesSource = (apiFetch: ApiFetch) => ({
  label: "USGS via IntelHub",
  async getSnapshot({ signal }: { signal?: AbortSignal } = {}) {
    const res = await apiFetch("/api/v1/gev/earthquakes", { signal });
    if (!res.ok) throw new Error(`IntelHub earthquakes HTTP ${res.status}`);
    const rows = await res.json();
    return Array.isArray(rows) ? rows : [];
  },
});
```

- [ ] **Step 5: index.ts 注册 `earthquakes: earthquakesSource(deps.apiFetch)`；`cargo check` + `npx vitest run` 全绿 + Commit**

`git commit -m "feat(gev): earthquakes endpoint + adapter (geo_events quake → GEV rows)"`

---

### Task 5: celestrak 组对齐 + TLE readGroup 端点 + 适配器

**Files:**
- Modify: `hub-core/crates/hub-core/src/monitor/sources/celestrak.rs`（GROUPS 改 GEV 六核心组）
- Modify: `hub-core/crates/hub-core/src/api.rs`（`/api/v1/gev/celestrak/{group}` 端点 + starlink 代理缓存）
- Create: `console/src/gev-adapters/satellites.ts`
- Test: 两端各自测试

**Interfaces:**
- Consumes: P1 `satellites` 表（`norad_id PK, name, category, tle_line1, tle_line2, epoch`）；`ctx.state.redis_timed`
- Produces: `GET /api/v1/gev/celestrak/{group}`（group ∈ stations|visual|gps-ops|glo-ops|galileo|geo）→ `text/plain` 三段式 TLE；`group=starlink` → hub 代理 celestrak.org（Redis 缓存 6h key `hub:globe:tle:starlink`）；未知组 404。适配器 `satellitesSource.readGroup(group,{signal})` → `{ok, status, text}`

- [ ] **Step 1: celestrak.rs GROUPS 修改 + 测试更新**

```rust
// 原: ["stations","visual","weather","gnss","military"]
// 新（GEV 对齐；starlink 按需走端点代理，不入 PG——P1 spec 决策延续）:
const GROUPS: &[&str] = &["stations", "visual", "gps-ops", "glo-ops", "galileo", "geo"];
```

更新 `celestrak_groups()` 测试断言为新数组。`weather/gnss/military` 组存量行会随新组全量替换自然存留（表按 category 全量替换的只是新组；旧 category 行不再更新——加一行迁移清理：`DELETE FROM satellites WHERE category IN ('weather','gnss','military')`，放 `hub-core/migrations/0019_satellites_gev_groups.sql`）。

- [ ] **Step 2: 端点 TDD（TLE 文本构造纯函数 + 组校验）**

```rust
fn tle_text(rows: &[(String, String, String)]) -> String { // (name, l1, l2)
    rows.iter().map(|(n,a,b)| format!("{n}\n{a}\n{b}")).collect::<Vec<_>>().join("\n") + "\n"
}
#[test]
fn tle_text_three_line_blocks() {
    let s = tle_text(&[("ISS".into(),"1 ..".into(),"2 ..".into())]);
    assert_eq!(s, "ISS\n1 ..\n2 ..\n");
}
const GEV_TLE_GROUPS: &[&str] = &["stations","visual","gps-ops","glo-ops","galileo","geo"];
// handler: group ∈ GEV_TLE_GROUPS → PG 取 category=group 全行 → tle_text → text/plain
//          group == "starlink" → redis GET hub:globe:tle:starlink；miss → GET
//          https://celestrak.org/NORAD/elements/gp.php?GROUP=starlink&FORMAT=tle
//          （ctx.http，与 celestrak.rs 同 client）→ SETEX 21600 → text/plain
//          否则 404 {error:"unknown group"}
```

- [ ] **Step 3: `cargo test -p hub-core tle_text` + `cargo check` 绿**

- [ ] **Step 4: 适配器（TDD）**

```ts
const GROUPS = new Set(["stations","visual","gps-ops","glo-ops","galileo","geo","starlink"]);
export const satellitesSource = (apiFetch: ApiFetch) => ({
  label: "CelesTrak via IntelHub",
  async readGroup(group: string, { signal }: { signal?: AbortSignal } = {}) {
    if (!GROUPS.has(group)) throw new TypeError("Unknown satellite group");
    const res = await apiFetch(`/api/v1/gev/celestrak/${group}`, { signal });
    return { ok: res.ok, status: res.status, text: res.ok ? await res.text() : "" };
  },
});
```

测试：未知组抛 TypeError；200 → `{ok:true, status:200, text}`；500 → `{ok:false, status:500, text:""}`。

- [ ] **Step 5: index.ts 注册 + 全绿 + Commit**

`git commit -m "feat(gev): celestrak GEV group alignment + readGroup endpoint (starlink proxied) + adapter"`

---

### Task 6: 航班 + 军机适配器（映射 P1 adsb 快照）

**Files:**
- Create: `console/src/gev-adapters/aircraft-map.ts`、`flights.ts`、`military.ts`
- Modify: `console/src/gev-adapters/index.ts`
- Test: `console/src/gev-adapters/__tests__/aircraft-map.test.ts`

**Interfaces:**
- Consumes: `GET /api/v1/globe/aircraft` envelope `{ts, count, coverage, cycle_secs, last_tick, aircraft:[{hex, flight, lat, lon, alt_m, gs, track, squawk, mil, seen, age_s}], stale?}`（**`gs`=节**）
- Produces: `flightsSource.getSnapshot(query,{signal})` → GEV envelope；`militarySource.getSnapshot({},{signal})` → 同形但 records 只含 `mil===true`

- [ ] **Step 1: 映射纯函数 TDD**

`aircraft-map.ts`：

```ts
export const KNOTS_TO_MPS = 0.514444;
export interface AdsbPoint { hex: string; flight: string; lat: number; lon: number;
  alt_m: number; gs: number; track: number; squawk: string; mil: boolean; seen: number; age_s: number; }
export function toGevRecord(p: AdsbPoint, observedAtMs: number) {
  return {
    id: p.hex.toLowerCase(), reference: p.hex,
    latitude: p.lat, longitude: p.lon,
    callsign: (p.flight ?? "").trim() || null, originCountry: null,
    positionTimeMs: observedAtMs - Math.round(p.age_s * 1000),
    contactTimeMs: observedAtMs,
    baroAltitudeM: p.alt_m, ellipsoidAltitudeM: null,
    onGround: false, // adsb.lol 快照无 ground 位；引擎按 alt<=0 自行降级
    speedMps: p.gs * KNOTS_TO_MPS, courseDeg: p.track, verticalRateMps: null,
    category: null, typeCode: null, registration: null, operator: null,
  };
}
export function toEnvelope(records: ReturnType<typeof toGevRecord>[],
                           env: { ts: string; coverage?: unknown; stale?: boolean },
                           source: string, nowMs: number) {
  const observedAtMs = Date.parse(env.ts);
  return { records, complete: true, rejectedCount: 0, source,
    coverage: env.coverage ?? null, observedAtMs,
    ageMs: nowMs - observedAtMs, stale: env.stale === true,
    freshness: env.stale === true ? "stale" : "live",
    status: env.stale === true ? "degraded" : "ok" };
}
```

测试：`toGevRecord` 单位换算（gs=450 → speedMps≈231.5）、age_s→positionTimeMs、callsign trim；`toEnvelope` stale→freshness/status。

- [ ] **Step 2: flights.ts / military.ts**

```ts
export const flightsSource = (apiFetch: ApiFetch) => ({
  label: "adsb.lol via IntelHub",
  async getSnapshot(_query: unknown, { signal }: { signal?: AbortSignal } = {}) {
    const res = await apiFetch("/api/v1/globe/aircraft", { signal });
    if (!res.ok) throw new Error(`IntelHub flights HTTP ${res.status}`);
    const env = await res.json();
    const ts = Date.parse(env.ts);
    return toEnvelope((env.aircraft ?? []).map((p: AdsbPoint) => toGevRecord(p, ts)), env, "intelhub-adsb", Date.now());
  },
});
export const militarySource = (apiFetch: ApiFetch) => ({
  label: "adsb.lol mil via IntelHub",
  async getSnapshot(_q: unknown, { signal }: { signal?: AbortSignal } = {}) {
    const res = await apiFetch("/api/v1/globe/aircraft", { signal });
    if (!res.ok) throw new Error(`IntelHub military HTTP ${res.status}`);
    const env = await res.json();
    const mil = (env.aircraft ?? []).filter((p: AdsbPoint) => p.mil === true);
    const ts = Date.parse(env.ts);
    return toEnvelope(mil.map((p: AdsbPoint) => toGevRecord(p, ts)), env, "intelhub-adsb-mil", Date.now());
  },
});
```

（flights 传 `points.map(p => toGevRecord(p, Date.parse(env.ts)))` 全量、military 传过滤后的映射数组；`getTrack`/`getIdentities` 不实现，引擎可选调用自动跳过。）

- [ ] **Step 3: index.ts 注册 + `npx vitest run` 绿 + Commit**

`git commit -m "feat(gev): flights + military adapters mapping P1 adsb snapshot (knots→m/s, age_s→epoch ms)"`

---

### Task 7: bootstrap 核心 `gev-boot/application.ts`

**Files:**
- Create: `console/src/gev-boot/request-services.ts`、`application.ts`
- Test: `console/src/gev-boot/__tests__/request-services.test.ts`

**Interfaces:**
- Consumes: T2 引擎 import；T3 `createIntelHubLayerSources`；`createApplication` / `createApplicationScene` / `createApplicationCatalog`（引擎 `src/app/`）
- Produces: `createIntelHubGlobe(opts: {apiFetch, googleApiKey, cesiumToken}): {start(): Promise<void>, destroy(): Promise<void>, getComponents(): any}`——HUD 页面（T8）只与这个签名交互

- [ ] **Step 1: 复核引擎校验面（执行时必读，非占位——这是对齐步骤）**

读 `console/gev-engine/src/services/requests.js`（`createApplicationRequestServices` 全量字段）与 `console/gev-engine/src/app/operations.js:10-17`（校验逻辑），把本任务 request-services.ts 的字段名单逐个对齐；同时读 `src/app/controls.js`、`src/app/data.js`、`src/app/tools.js` 三个 factory 的入参要求。

- [ ] **Step 2: request-services.ts（最小可用面 + 失败测试）**

```ts
export function createIntelHubRequestServices(apiFetch: ApiFetch) {
  return {
    boundaries: { query: async () => null },              // 国界查询 P5 再接
    terrain: { getHeights: async () => [] },              // 地形采样禁用（引擎 terrain 本已禁用）
    regional: { getBrief: async () => null },
    weather: { getConditions: async () => null },         // P4 接 Open-Meteo
    summary: { summarize: async () => null },
    features: { geocode: false, routing: false, llm: false },
  };
}
```

测试：字段存在性与可调用性（逐字段 `typeof === "function"` 或 boolean）。

- [ ] **Step 3: application.ts**

```ts
// @ts-expect-error vendored JS engine
import { createApplication } from "../../gev-engine/src/app/application.js";
// @ts-expect-error vendored JS engine
import { createApplicationScene } from "../../gev-engine/src/app/scene.js";
// @ts-expect-error vendored JS engine
import { createApplicationCatalog } from "../../gev-engine/src/app/constructCatalog.js";
// @ts-expect-error vendored JS engine
import { setScopeMaskEnabled } from "../../gev-engine/src/scopeMask.js";
import { createIntelHubRequestServices } from "./request-services";
import { createIntelHubLayerSources } from "../gev-adapters";

export function createIntelHubGlobe(opts: {
  apiFetch: Parameters<typeof createIntelHubLayerSources>[0]["apiFetch"];
  googleApiKey: string; cesiumToken: string;
}) {
  const app = createApplication({
    createScene: ({ signal, defer }: any) =>
      createApplicationScene({
        requestServices: createIntelHubRequestServices(opts.apiFetch),
        googleApiKey: opts.googleApiKey, cesiumToken: opts.cesiumToken,
        loaderStatus: document.querySelector("#loading-screen .loader-status"),
        signal, defer,
      }),
    createControls: ({ scene, signal, defer }: any) => {
      const sources = createIntelHubLayerSources({ apiFetch: opts.apiFetch });
      const catalog = createApplicationCatalog({
        surface: scene.operations.surface, sources, signal,
      });
      (scene as any).catalog = catalog; // T9-T11 经 getComponents().scene.catalog 访问
      return { catalog };
    },
    createData: async () => ({}),   // 引擎 createApplicationData 绑 #data-toggles 渲染开关——T9 自研栏后视需要再接线
    createTools: async () => ({}),
  });
  return {
    start: async () => { await app.start(); setScopeMaskEnabled(false); },
    destroy: () => app.destroy(),
    getComponents: () => app.getComponents(),
  };
}
```

注意：catalog 构造在 controls 阶段（scene 之后有 surface）；若引擎的 data/tools 阶段对 catalog 有硬依赖（Step 1 复核发现），改为在 data 阶段建 catalog 或调用引擎 `createApplicationControls`（`src/app/controls.js`）——**以 Step 1 复核结论为准调整阶段归属**，签名不变。

- [ ] **Step 4: `npx tsc --noEmit` + `npx vitest run src/gev-boot` 绿 + Commit**

`git commit -m "feat(gev): bootstrap core — own lifecycle wiring, scopeMask off, no standalone modules"`

---

### Task 8: HUD 页面骨架 + 路由替换

**Files:**
- Create: `console/src/pages/GlobeV2.tsx`、`console/src/globe-hud/HudFrame.tsx`、`console/src/globe-hud/hud.css`
- Modify: `console/src/App.tsx`（/globe → GlobeV2 lazy）
- Test: `console/src/globe-hud/__tests__/hud-frame.test.tsx`

**Interfaces:**
- Consumes: T7 `createIntelHubGlobe`；P1 `ErrorBoundary`（`console/src/components/ErrorBoundary.tsx`）
- Produces: `GlobeV2` 页面组件；DOM 契约：容器内必含 `#loading-screen>.loader-status`、`#cesiumContainer`、`#data-toggles`、四边 HUD 挂载点 `data-hud="top|left|right|bottom"`

- [ ] **Step 1: RTL 失败测试（jsdom，mock bootstrap 防 WebGL）**

```tsx
vi.mock("../../gev-boot/application", () => ({
  createIntelHubGlobe: () => ({ start: vi.fn(async () => {}), destroy: vi.fn(async () => {}),
    getComponents: () => ({}) }),
}));
test("mounts four-edge frame + engine DOM contract", () => {
  render(<MemoryRouter><GlobeV2 /></MemoryRouter>);
  expect(document.querySelector("#cesiumContainer")).toBeInTheDocument();
  expect(document.querySelector("#loading-screen .loader-status")).toBeInTheDocument();
  expect(document.querySelector("#data-toggles")).toBeInTheDocument();
  for (const edge of ["top","left","right","bottom"])
    expect(document.querySelector(`[data-hud="${edge}"]`)).toBeInTheDocument();
});
```

- [ ] **Step 2: HudFrame.tsx（四边框架 + 引擎 DOM + 折叠态）**

```tsx
export function HudFrame({ children }: { children?: React.ReactNode }) {
  return (
    <div className="hud-root">
      <div id="cesiumContainer" className="hud-canvas" />
      <div id="loading-screen"><span className="loader-status" /></div>
      <div id="data-toggles" hidden />
      <div className="hud-edge hud-top" data-hud="top" />
      <div className="hud-edge hud-left" data-hud="left" />
      <div className="hud-edge hud-right" data-hud="right" />
      <div className="hud-edge hud-bottom" data-hud="bottom" />
      {children}
    </div>
  );
}
```

`hud.css`：`.hud-root{position:fixed;inset:0;background:#030509}` `.hud-canvas{position:absolute;inset:0}` 四边 `position:absolute` + 圆角 + 半透明暗底 + `backdrop-filter:blur(4px)`；左右栏 `.collapsed` 宽度 0 过渡；`#loading-screen{position:absolute;inset:auto 8px 8px auto;opacity:.6}`（引擎写 loaderStatus 文本，低调呈现）。

- [ ] **Step 3: GlobeV2.tsx（StrictMode 双挂载防护 + ErrorBoundary + bootstrap 生命周期）**

```tsx
const booted = { current: false }; // module-level：引擎单例，一页一实例
export default function GlobeV2() {
  const [error, setError] = useState<string | null>(null);
  useEffect(() => {
    if (booted.current) return;           // StrictMode 第二挂载直接跳过
    booted.current = true;
    const globe = createIntelHubGlobe({
      apiFetch: makeApiFetch("", apiToken()), // 同源；token 读取复用 console 现有 auth 工具
      googleApiKey: import.meta.env.VITE_GOOGLE_MAPS_KEY ?? "",
      cesiumToken: import.meta.env.VITE_ION_KEY ?? "",
    });
    globe.start().catch(e => setError(String(e)));
    return () => { /* 不 destroy：引擎单例随页面常驻；路由离开时 destroy 由 P3 决策 */ };
  }, []);
  if (error) return <div className="hud-fatal">Globe engine failed: {error}</div>;
  return <HudFrame />;
}
```

路由：`/globe` → `lazy(() => import("./pages/GlobeV2"))`，外层包 P1 `ErrorBoundary`。`apiToken()`：复用 console 现有 token 来源（执行时 grep `Authorization` 确认现有工具函数名并复用，禁止新造）。

- [ ] **Step 4: 测试绿 + `npm run build` 绿 + Commit**

`git commit -m "feat(gev): GlobeV2 HUD frame + route swap (four-edge, singleton-safe)"`

---

### Task 9: HUD 图层栏（域图标 + 悬停飞出）

**Files:**
- Create: `console/src/globe-hud/domains.ts`、`HudLayerRail.tsx`
- Test: `console/src/globe-hud/__tests__/hud-layer-rail.test.tsx`

**Interfaces:**
- Consumes: T8 `data-hud="left"` 挂载点；引擎 catalog（经 `getComponents().scene.catalog`）——**执行时先读 `console/gev-engine/src/app/catalog.js` 的 `createLayerCatalog` 返回面**（getAll/setEnabled 实际名字）与 `src/data/layerState.js` 的 `LAYER_STATE_REGISTRY`（id/token 列表）
- Produces: `DOMAINS: [{id, icon, label zh/en, layers: string[]}]`（7 域）；`HudLayerRail` 组件

- [ ] **Step 1: 失败测试——7 域映射覆盖 catalog 全部图层 id**

```ts
const DOMAIN_LAYERS: Record<string, string[]> = {
  air: ["flights", "military"],
  space: ["satellites", "launches"],
  ground: ["traffic", "cctv", "earthquakes", "alpr"],
  sea: ["vessels", "cables"],
  cyber: ["installations"],       // 军事设施/基础设施归基建域
  infra: ["installations", "cables"],
  env: ["firms", "bikeshare", "radio"],
};
```

注意：上表是**初稿**——Step 1 先读 `LAYER_STATE_REGISTRY` 拿到真实图层 id 全集，测试断言「每个 registry id 恰好属于一个域」，按真实 id 修正映射（cyber/infra 域可合并为 6-7 域，以 HUD 三票的 7 域为上限）。

- [ ] **Step 2: HudLayerRail.tsx**

域图标竖列（emoji 占位 ✈🛰🏙🚢🌐🏭🔥），hover/click 展开 flyout（绝对定位在栏右侧），flyout 内该域图层复选列表；toggle 调 catalog 的启用 API；栏体支持折叠（点击把手切换 `.collapsed`，鼠标离开 flyout 3s 自动收起）。组件从 props 接 `catalog`（GlobeV2 经 state 传入，避免引擎单例直接 import）。

- [ ] **Step 3: 测试绿（toggle 调用断言用 mock catalog）+ Commit**

`git commit -m "feat(gev): HUD layer rail — 7 domains, flyout toggles, auto-collapse"`

---

### Task 10: HUD 详情面板（右侧常驻）

**Files:**
- Create: `console/src/gev-boot/context-bridge.ts`、`console/src/globe-hud/HudDetailPanel.tsx`
- Test: `console/src/globe-hud/__tests__/hud-detail-panel.test.tsx`

**Interfaces:**
- Consumes: 引擎 `src/data/contextStore.js`（selection/tracking store——执行时读其 export 面）；T8 `data-hud="right"`
- Produces: `useGlobeSelection(): {kind: "flight"|"satellite"|"quake"|null, data: any}` React hook（context-bridge 把引擎 store 订阅转 React state）

- [ ] **Step 1: context-bridge.ts + 失败测试**

```ts
// 执行时读 console/gev-engine/src/data/contextStore.js 的订阅 API（名字以 vendor 为准），
// 桥接为 React useSyncExternalStore：
export function useGlobeSelection() { /* useSyncExternalStore(subscribe, getSnapshot) */ }
```

测试：mock contextStore，选中 flight 对象 → hook 返回 `{kind:"flight", data}`。

- [ ] **Step 2: HudDetailPanel.tsx 三模板**

flight：callsign、高度 m、速度 km/h（speedMps×3.6）、航向、关联动作 `[进图谱]`（链接 `/graph?q={callsign}`）；satellite：name、NORAD、group；quake：mag、place、time（相对时间）。空态显示「点击地球上的目标查看详情」。面板右上折叠按钮。

- [ ] **Step 3: 测试绿 + Commit**

`git commit -m "feat(gev): HUD detail panel — selection bridge + per-kind templates"`

---

### Task 11: HUD 顶条 + 底条

**Files:**
- Create: `console/src/globe-hud/HudTopBar.tsx`、`HudBottomBar.tsx`
- Test: `console/src/globe-hud/__tests__/hud-bars.test.tsx`

**Interfaces:**
- Consumes: `GET /api/v1/overview`（现有，采集器健康+计数）；T8 顶/底挂载点
- Produces: 顶条（UTC 时钟秒级、告警铃铛计数、搜索占位 disabled 标注"P5"）；底条（globe 相关源健康点 adsb/celestrak/usgs/opensky + 对象计数）

- [ ] **Step 1: 失败测试**（mock overview：健康点渲染数、告警计数、UTC 时钟存在）

- [ ] **Step 2: 实现**——overview 15s 轮询（复用 console 现有 fetch 模式）；健康点颜色 state→绿/黄/红；点击底条源名跳 `/monitor`

- [ ] **Step 3: 测试绿 + Commit** `git commit -m "feat(gev): HUD top/bottom bars (UTC clock, alerts, source health)"`

---

### Task 12: 契约守卫测试

**Files:**
- Create: `console/src/gev-boot/__tests__/source-contracts.test.ts`

**Interfaces:**
- Consumes: `console/gev-engine/src/app/constructCatalog.js`（文本读取）；T3 工厂
- Produces: 上游改契约时红灯的守卫

- [ ] **Step 1: 实现 + 测试**

```ts
import { readFileSync } from "node:fs";
import { createIntelHubLayerSources } from "../../gev-adapters";
test("adapters cover upstream SOURCE_METHODS verbatim", () => {
  const src = readFileSync(new URL("../../../gev-engine/src/app/constructCatalog.js", import.meta.url), "utf8");
  const block = src.match(/SOURCE_METHODS\s*=\s*Object\.freeze\(\{([\s\S]*?)\}\)/)?.[1] ?? "";
  const entries = [...block.matchAll(/(\w+):\s*\[([^\]]*)\]/g)]
    .map(m => [m[1], m[2].match(/'(\w+)'/g)?.map(s => s.slice(1, -1)) ?? []] as const);
  expect(entries.length).toBeGreaterThanOrEqual(14);
  const sources = createIntelHubLayerSources({ apiFetch: async () => new Response("{}") });
  for (const [layer, methods] of entries)
    for (const m of methods)
      expect(typeof (sources as any)[layer]?.[m], `${layer}.${m}`).toBe("function");
});
```

- [ ] **Step 2: 人为破坏验证**——临时从 stubs 删一个方法 → 测试红 → 还原。Commit `test(gev): source contract guard (upstream churn fuse)`

---

### Task 13: OpenSky OAuth 采集器扩展（env-gated，无 key 时 idle）

**Files:**
- Modify: `hub-core/crates/hub-core/src/monitor/sources/opensky.rs`
- Test: 同文件 `#[cfg(test)]`

**Interfaces:**
- Consumes: env `HUB_OPENSKY_CLIENT_ID`/`HUB_OPENSKY_CLIENT_SECRET`（`core/secrets.env`，缺席 → 本采集器 idle 直接返 `Ok(vec![])` 且健康格 detail 注明 "no key, shelved"）；P1 `adsb.rs` 的 `AdsbPoint`/`AIRCRAFT_KEY`/merge 语义
- Produces: 全量 state vectors（`GET https://opensky-network.org/api/states/all`，OAuth client-credentials token 缓存 25min）→ 与 adsb.lol 轮转快照**按 hex 合并（fresher wins）写同一 `hub:globe:aircraft`**；`mil` 判定沿用 adsb 来源标记（opensky 行 mil=false）

- [ ] **Step 1: 纯函数 TDD**

```rust
// parse_state_vectors(json) -> Vec<AdsbPoint>：
// state vector 数组序: [0]icao24 [1]callsign [5]lon [6]lat [7]baro_altitude(m)
// [8]on_ground [9]velocity(m/s) [10]true_track [11]vertical_rate [3]time_position [4]last_contact
// 单位: velocity m/s → gs(knots) = ×1.94384；on_ground=true 的行丢弃（与快照语义一致）
// token_cache_valid(fetched_at, expires_in, now) -> bool（提前 5min 过期）
```

- [ ] **Step 2: 实现**——token：`POST https://auth.opensky-network.org/auth/realms/opensky-network/protocol/openid-connect/token`（form: grant_type=client_credentials）；间隔 30s（匿名 10s 但 OAuth 配额足，30s 与 flights layer updateInterval 对齐）；merge 复用 `adsb::merge_aircraft`（`pub(crate)` 已开）

- [ ] **Step 3: `cargo test -p hub-core opensky` 绿 + Commit** `feat(gev): opensky OAuth full state vectors (env-gated, merged snapshot)`

---

### Task 14: probe v2（HUD + 引擎冒烟）

> **实际落点（T14 执行时修正）**：v2 断言写进新文件 `console/probe-gev.mjs`；
> `console/probe-globe.mjs` 只保留 13 行 passthrough 委托（防旧调用路径误绿）。
> T16 已删除 `probe-globe.mjs`，**此后统一跑 `node console/probe-gev.mjs <base> <key>`**。

**Files:**
- Modify: `console/probe-globe.mjs`（实现落到新文件 `console/probe-gev.mjs`）

**Interfaces:**
- Consumes: T8-T11 DOM；引擎 debug handle `window.__godsEyeView`（`src/app/tools.js:105-118` 设置——T7 若未走引擎 tools 阶段则无此 handle，probe 改断言 REST + DOM）
- Produces: `probe-gev OK` 输出含 hud=✓ canvas=✓ aircraft=N satellites=N pageerrors=0

- [ ] **Step 1: 扩展断言**

```js
const hud = await page.evaluate(() => ({
  top: !!document.querySelector('[data-hud="top"]'),
  left: !!document.querySelector('[data-hud="left"]'),
  right: !!document.querySelector('[data-hud="right"]'),
  bottom: !!document.querySelector('[data-hud="bottom"]'),
  canvas: !!document.querySelector("#cesiumContainer canvas"),
}));
// 全 true 否则 process.exit(1) 打印缺失项；保留 P1 的 REST 计数与 pageerror 过滤（已收窄的正则不动）
```

- [ ] **Step 2: 对 315 跑通（T17 前置）+ Commit** `test(gev): probe v2 — HUD frame + engine canvas assertions`

---

### Task 15: sp6 验收 +N（gev 面）

**Files:**
- Modify: `scripts/accept-sp6.py`（globe 块后追加 gev 段）

**Interfaces:**
- Consumes: T4/T5 端点；T13 健康格
- Produces: 新增检查：①`gev: earthquakes endpoint rows>0`（24h 内全球 M2.5+ 几乎恒 >0；若 0 则打印明细不直接 fail？——按既有 check() 语义 fail，usgs 采集器本就活跃）②`gev: celestrak readGroup stations TLE text`（body 含 `\n1 ` 且行数>30）③`gev: starlink proxied TLE`（200 且含 `STARLINK`）④`gev: flights snapshot envelope`（/api/v1/globe/aircraft count>100，复用既有逻辑合并为一条新检查）⑤opensky 健康格：key 缺席 → `check_shelved("opensky OAuth configured", "HUB_OPENSKY_CLIENT_ID absent")`，存在 → 硬断言 detail ok

- [ ] **Step 1: 追加检查（紧随既有 globe 块，沿用 `check()`/`check_shelved()`/`req()`/`redis()` helpers，插到 summary 打印之前）**

- [ ] **Step 2: `python3 -c "import ast; ast.parse(...)"` + 对 315 空跑结构验证 + Commit** `test(gev): sp6 +5 gev acceptance`

---

### Task 16: P1 前端退役

**Files:**
- Delete: `console/src/pages/Globe.tsx`、`console/src/globe/aircraft.ts`、`console/src/globe/satellites.ts`、`console/src/globe/basemap.ts`、`console/probe-globe.mjs`（T14 的 passthrough，退役）
- Modify: `console/src/App.tsx`（导航文案 "Globe" 指向新页 —— T8 已完成 `/globe` → GlobeV2，App.tsx 无需再改）；`console/probe-gev.mjs` 头部注释去掉对已删文件的描述
- **保留**：`console/src/basemap.ts`（顶层的 2D 雷达底图，Radar.tsx / MonitorMap.tsx 在用）——不属于 P1 globe 体系

**Interfaces:**
- Consumes: T8 路由替换已完成
- Produces: 无 P1 前端残留；**保留** `/api/v1/globe/*` 后端（T6 适配器依赖）与 `scripts/accept-sp6.py` 既有 globe 检查

- [ ] **Step 1: 引用扫描** `grep -rn "from.*globe/aircraft\|from.*globe/satellites\|from.*globe/basemap\|pages/Globe\"" console/src/` → 除 App.tsx 已改外应为空

- [ ] **Step 2: 删除 + `npx tsc --noEmit` + `npm run build` 绿 + Commit** `refactor(gev): retire P1 self-built globe frontend (backend endpoints retained)`

---

### Task 17: 315 部署 + 验收（**完成后暂停，等用户放行**）

- [ ] **Step 1: rsync → 315 → build-hub + build-console → restart**（AGENTS.md 序列；rsync 追加 `--exclude '.superpowers/'`；确认 `.build-manifest.json` 含 `cesium_ion`/`google_maps` 字段）
- [ ] **Step 2: 等 90s 采集器首轮 → sp6/sp8/sp7/sp3 全绿**（KEY 用 TSV awk 提取——`awk -F"\t" '/^[^#]/{print $3; exit}' core/agent-keys.txt`）
- [ ] **Step 3: probe-gev OK**（`node console/probe-gev.mjs <base> <key>`；`npx playwright install chromium` 如需要）
- [ ] **Step 4: 手动体验核对**——浏览器开 `http://10.10.10.35:8800/globe`：五图层可开关、点击有详情、无黑屏
- [ ] **Step 5: 暂停，向用户报告并请求 merge 放行**

---

### Task 18: merge → 410 → push + 基线更新

- [ ] **Step 1: 用户放行后**：worktree `git add -A && git commit` → 主仓库 `git merge --no-ff feat/gev-engine` → `git worktree remove ../IntelHub-gev` + `git branch -d`
- [ ] **Step 2: 410 同序列部署 + 生产验收 + probe**
- [ ] **Step 3: AGENTS.md 基线更新（sp6 项数）+ `git push origin main`**
- [ ] **Step 4: 记忆更新**（intelhub.gev-p2：pin SHA、五图层、退役事项、P3 待办）

---

## Self-Review 记录（controller 已执行）

- **Spec 覆盖**：vendor/同步（T1-T2+T12）、适配器四层分工（T3-T7）、HUD 三票（T8-T11）、契约守卫（T12）、波次 1 五图层（T4-T6 后端+适配、3D 城市引擎自带、T13 增强）、P1 退役（T16）、验收部署（T14-T15-T17-T18）✓；P3-P5 图层 stub 占位（T3）✓
- **类型一致性**：`createIntelHubLayerSources`/`createIntelHubGlobe`/`toGevRecord`/`toEnvelope`/`readGroup→{ok,status,text}`/quake row 七字段 跨任务一致 ✓
- **已知执行期复核点（非占位，均有回退方案）**：T7 catalog 阶段归属、T9 catalog API 实名、T10 contextStore 订阅面、T3 stub 返回形状——四处均以「读 vendor 实际代码」为首步
- T8 review: mgrs 依赖删除（引入链不可达）；P3 接 engine controls/ui（app/controls.js→ui/composition.js→hud.js:18）时需重装 + egm96-universal 已验证必需
